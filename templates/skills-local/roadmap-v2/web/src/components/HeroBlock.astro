---
import type { PageBlock } from '../lib/page-spec';

interface Props {
  block: PageBlock;
}

const { block } = Astro.props;
---
<section class="hero" id={block.id}>
  <wa-tag>{block.type}</wa-tag>
  {block.title && <h1>{block.title}</h1>}
  {block.text && <p>{block.text}</p>}
</section>
