---
import RoadmapLayout from './RoadmapLayout.astro';
import type { PageSpec } from '../lib/page-spec';

interface Props {
  spec: PageSpec;
}

const { spec } = Astro.props;
---
<RoadmapLayout spec={spec}>
  <slot />
</RoadmapLayout>
