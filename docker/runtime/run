#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(CDPATH= cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd -P)"
REPO_ROOT="$(CDPATH= cd -- "${SCRIPT_DIR}/../.." && pwd -P)"

DEFAULT_IMAGE="my-roadmap-roadmap-runtime:playwright-1.60.0"
IMAGE="${ROADMAP_RUNTIME_IMAGE:-${DEFAULT_IMAGE}}"
NETWORK_MODE="${ROADMAP_RUNTIME_NETWORK:-none}"
IPC_MODE="${ROADMAP_RUNTIME_IPC:-host}"
DEFAULT_BUILD_COMMAND="DOCKER_BUILDKIT=1 docker build --tag ${DEFAULT_IMAGE} docker/runtime"

usage() {
  cat <<'USAGE'
Uso:
  docker/runtime/run <comando> [args...]

Exemplos:
  docker/runtime/run python3 .codex/skills/roadmap/roadmap-page/scripts/check_roadmap_html_shape.py --html .tmp/roadmaps/exemplo/roadmap.html
  docker/runtime/run node .codex/skills/roadmap/roadmap-page/scripts/check_roadmap_visual_render.mjs --roadmap-dir .tmp/roadmaps/exemplo

Variaveis:
  ROADMAP_RUNTIME_IMAGE    Nome/tag da imagem. Padrao: my-roadmap-roadmap-runtime:playwright-1.60.0
  ROADMAP_RUNTIME_NETWORK  Modo de rede do docker run. Padrao: none
  ROADMAP_RUNTIME_IPC      Modo IPC do docker run. Padrao: host
USAGE
}

if [[ "$#" -eq 0 ]]; then
  usage >&2
  exit 2
fi

python3 "${SCRIPT_DIR}/util/docker_preflight.py" \
  --image "${IMAGE}" \
  --default-image "${DEFAULT_IMAGE}" \
  --build-command "${DEFAULT_BUILD_COMMAND}" \
  --wrapper-label "docker/runtime/run"

docker_run_args=(
  --rm
  --init
  --workdir /workspace
  --user "$(id -u):$(id -g)"
  --env HOME=/tmp
  --env XDG_CACHE_HOME=/tmp/.cache
  --env npm_config_cache=/tmp/.npm
  --env PLAYWRIGHT_BROWSERS_PATH=/ms-playwright
  --tmpfs /tmp:exec,mode=1777
  --mount "type=bind,source=${REPO_ROOT},target=/workspace"
)

if [[ -n "${NETWORK_MODE}" ]]; then
  docker_run_args+=(--network "${NETWORK_MODE}")
fi

if [[ -n "${IPC_MODE}" ]]; then
  docker_run_args+=(--ipc "${IPC_MODE}")
fi

exec docker run "${docker_run_args[@]}" "${IMAGE}" "$@"
